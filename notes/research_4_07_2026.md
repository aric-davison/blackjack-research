Today we added some new features to our blackjack simulation framework. We implemented a new function to export the computed strategies to CSV files, allowing for easier analysis and sharing of results. We also updated our main simulation scripts to save the strategy tables after each run, ensuring that we have a record of the strategies generated by each algorithm. Finally, we added some logging to indicate where the strategy tables are saved, making it easier to locate them for future reference.

Findings in our data. our simulations diverged from the optimal strategy in certain scenarios, during the split ev calculations we found that the strategy tables generated by our algorithms had some discrepancies compared to the optimal strategy, particularly in situations involving splits. We are calculating ev's for split pairs against aces without accounting for the dealer checking the hole card. this is very interisting because im learning more and more about blackjack strategy. we probably need to account for this because this is a standard in most casinos. 